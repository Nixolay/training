package companies

import (
	"testing"
)

// -------------- Задачка из банка вопросов Т-Банка --------------------
// 📌 Задачка из банка вопросов Т-Банка
// 📝 Дан массив, представляющий ряд сидений, где seats[i] = 1 означает,
//
//	что на i-м месте сидит человек, а seats[i] = 0 означает, что i-е место свободно (индексация начинается с 0).
//
// Гарантируется наличие хотя бы одного свободного места и хотя бы одного занятого.
// Алекс хочет сесть на место таким образом, чтобы расстояние до ближайшего к нему человека было максимальным.
// Верните это максимальное расстояние до ближайшего человека.
//
// Пример:
// Вход: seats = [1,0,0,0,1,0,1]
// Выход:
//  2. (Если Алекс сядет на второе свободное место (т.е. seats[2]), то ближайший человек будет на расстоянии
//  2. Если он сядет на любое другое свободное место, ближайший человек будет на расстоянии
//  1. Таким образом, максимальное расстояние до ближайшего человека равно 2)
//
// ⏰ O(n) — один проход с hash-map.
// 💾 O(1) — храним все числа в hash-map.
// 🔑 Идея: Разбить массив на сегменты между занятыми местами и найти оптимальное место в каждом сегменте.
// 🎯 Цель: Найти такое свободное место, где расстояние до ближайшего человека максимально.
func TestMaxDistToClosest(t *testing.T) {
	tests := []struct {
		name  string
		seats []int
		want  int
	}{
		{
			name:  "Простой случай - один человек в начале",
			seats: []int{1, 0, 0, 0},
			want:  3,
		},
		{
			name:  "Простой случай - один человек в конце",
			seats: []int{0, 0, 0, 1},
			want:  3,
		},
		{
			name:  "Один человек в середине",
			seats: []int{0, 0, 1, 0, 0},
			want:  2,
		},
		{
			name:  "Два человека - максимальное расстояние 1",
			seats: []int{1, 0, 0, 0, 1},
			want:  2,
		},
		{
			name:  "Два человека - максимальное расстояние 2",
			seats: []int{1, 0, 0, 0, 0, 1},
			want:  2,
		},
		{
			name:  "Три человека",
			seats: []int{1, 0, 0, 1, 0, 0, 1},
			want:  1,
		},
		{
			name:  "Максимальное расстояние от начала",
			seats: []int{0, 0, 0, 1, 0, 0},
			want:  3,
		},
		{
			name:  "Максимальное расстояние в конце",
			seats: []int{1, 0, 0, 0, 0, 0},
			want:  5,
		},
		{
			name:  "Минимальный массив - 2 места",
			seats: []int{1, 0},
			want:  1,
		},
		{
			name:  "Минимальный массив - 2 места, пустое начало",
			seats: []int{0, 1},
			want:  1,
		},
		{
			name:  "Все места заняты",
			seats: []int{1, 1, 1, 1},
			want:  0,
		},
		{
			name:  "Чередующиеся места",
			seats: []int{1, 0, 1, 0, 1},
			want:  1,
		},
		{
			name:  "Сложный случай",
			seats: []int{0, 1, 0, 0, 0, 0, 0, 1, 0},
			want:  3,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if got := MaxDistToClosest(tt.seats); got != tt.want {
				t.Errorf("maxDistToClosest() = %v, want %v", got, tt.want)
			}
		})
	}
}

func MaxDistToClosest(seats []int) int {
	n := len(seats)
	maxDist := 0
	lastPerson := -1

	for i := 0; i < n; i++ {
		if seats[i] == 1 {
			if lastPerson == -1 {
				// Расстояние от начала до первого человека
				maxDist = i
			} else {
				// Расстояние между двумя людьми (середина)
				maxDist = max(maxDist, (i-lastPerson)/2)
			}

			lastPerson = i
		}
	}

	// Расстояние от последнего человека до конца
	return max(maxDist, n-1-lastPerson)
}

func max(a, b int) int {
	if a > b {
		return a
	}
	return b
}
