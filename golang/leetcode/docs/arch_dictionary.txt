📘 Словарь по системному дизайну (архитектура, БД, сервисы, очереди)

Монолит — единое приложение, в котором вся логика в одном коде и БД.
Микросервисы — набор независимых сервисов, взаимодействующих по API.
SOA (Service-Oriented Architecture) — архитектурный стиль, предшественник микросервисов.
Слабая связанность (loose coupling) — минимальные зависимости между компонентами.
Высокая связность (high cohesion) — модуль отвечает за одну задачу.

Базы данных
ACID — атомарность, согласованность, изолированность, долговечность.
CAP-теорема — система может обеспечивать только 2 из 3: Consistency, Availability, Partition tolerance.
Sharding — горизонтальное разбиение данных по нескольким БД.
Replication — копирование данных на несколько серверов для отказоустойчивости.
Primary-Replica — одна основная БД для записи, несколько для чтения.
Failover — автоматическое переключение на резерв при сбое.
OLTP — системы обработки транзакций (онлайн-платежи).
OLAP — аналитические системы для больших объёмов данных.
Index — структура (B-Tree, Hash) для ускорения поиска.
Partitioning — разбиение таблиц на части (по диапазону, по хэшу).

Кэширование
Cache — быстрый доступ к данным (в памяти).
Redis — in-memory key-value store, поддерживает структуры данных.
Memcached — простейший in-memory key-value cache.
Write-through cache — запись сразу в кэш и БД.
Write-back cache — запись в кэш, а позже в БД.
TTL (time-to-live) — время жизни записи в кэше.

Сервисы и API
REST — архитектурный стиль API с методами HTTP.
GraphQL — язык запросов к API, клиент сам выбирает поля.
gRPC — RPC поверх HTTP/2 с protobuf.
Service Discovery — поиск сервисов в динамической среде (Consul, etcd).
API Gateway — единая точка входа для всех сервисов.
Load Balancer — распределяет трафик между серверами.
Reverse Proxy — принимает запросы и перенаправляет их на backend.

Очереди и Kafka
Message Queue — асинхронная передача сообщений (RabbitMQ, Kafka).
Kafka — распределённая commit log система для потоков событий.
Topic — канал сообщений в Kafka.
Partition — разделение топика для параллельной обработки.
Producer — пишет сообщения в Kafka.
Consumer — читает сообщения.
Consumer Group — несколько консьюмеров делят нагрузку.
Offset — позиция сообщения в партиции.
Retention — время хранения сообщений.
Exactly-once delivery — гарантированная доставка без дубликатов.
Надёжность и масштабирование
Scalability — способность системы выдерживать рост нагрузки.
Horizontal scaling — увеличение числа серверов.
Vertical scaling — увеличение ресурсов одного сервера.
High Availability (HA) — устойчивость к сбоям.
Load Shedding — сброс избыточных запросов для защиты системы.
Rate limiting — ограничение числа запросов.
Circuit Breaker — предотвращает постоянные вызовы упавшего сервиса.
Retry Policy — стратегия повторных запросов (с backoff).
Idempotency — повторный вызов не меняет результат.